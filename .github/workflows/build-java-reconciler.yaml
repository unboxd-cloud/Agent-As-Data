name: Build Java Reconciler Image

on:
  push:
    branches:
      - main
    paths:
      - operator-java/**
      - .github/workflows/build-java-reconciler.yaml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Java reconciler
        uses: docker/build-push-action@v6
        with:
          context: operator-java
          push: true
          tags: ghcr.io/unboxd-cloud/agent-as-data-reconciler-java:latest
