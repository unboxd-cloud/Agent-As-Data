# Unboxd Fabric Agent SDK for Python

Python SDK for declaring and observing Agent-as-Data Fabric platform state.

Fabric is the Platform. Fabric provides Runtime and Flow.

SDKs declare and observe. The reconciler reconciles. SurrealDB stores truth. Fabric executes flow.
